/**
 * Constants for model selector calculations
 */

// Bucket method constants
export const BUCKET_VOLUME_LITERS = 10;
export const BUCKET_VOLUME_ML = 600; // 10L * 60 seconds = 600 for L/min calculation
export const DEFAULT_SECONDS = 60;
export const SECONDS_MIN = 40;
export const SECONDS_MAX = 200;

// Flow rate formatting
export const FLOW_RATE_DECIMAL_THRESHOLD = 10; // Show 0 decimals if >= 10

